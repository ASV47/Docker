name: my fisrt workflow
on:
  push:
   branches: main
jobs:
   fisrt_job:
      runs-on: ubuntu-latest
      steps:
         - name: Checkout code
           uses: actions/checkout@v4
         - name: bash script
           run: echo "hello world"
